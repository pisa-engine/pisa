#pragma once

#include <string>

namespace pisa {

namespace porter2 {
    std::string stem(const std::string &word);
}
namespace krovetz {
    std::string stem(const std::string &word);
}

} // namespace pisa
