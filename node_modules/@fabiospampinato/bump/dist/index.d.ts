import { Options } from './types';
declare function bump({ version, changelog, commit, tag, release }?: Options): Promise<void>;
declare const _default: typeof bump & {
  default: typeof bump;
}
declare namespace _default {
  export type type = typeof bump;
}
export = _default;
