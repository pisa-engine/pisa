declare type Bump = {
    commits: Commit[];
    version: string;
};
declare type Commit = {
    author_email: string;
    author_name: string;
    date: string;
    hash: string;
    message: string;
    isBump?: boolean;
};
declare type Options = {
    version?: boolean;
    changelog?: boolean;
    commit?: boolean;
    tag?: boolean;
    release?: boolean;
};
export { Bump, Commit, Options };
